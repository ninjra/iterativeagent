# ActivityMemory

A minimal .NET 8 WPF + core library + tests scaffold for fast, agent-friendly iteration.

## 30-second quickstart

```powershell
# From repo root
.\dev.ps1 bootstrap
.\dev.ps1 check
.\dev.ps1 run
```

## What’s inside

- WPF desktop app (`src/ActivityMemory.App`)
- Core library (`src/ActivityMemory.Core`)
- xUnit tests (`tests/ActivityMemory.Tests`)
- Local toolchain via `dotnet tool restore` (dotnet-format)
- One-command dev loop via `dev.ps1`

## Useful commands

```powershell
.\dev.ps1 format
.\dev.ps1 build
.\dev.ps1 test
.\dev.ps1 clean
```
