# Worklog

## 2025-09-27
- Attempted to run `dotnet --info` in the container but `dotnet` was not available (`command not found`). Documenting environment limitation for later follow-up.
- Bootstrapped initial repo scaffold (solution, projects, scripts, CI, docs) to match requested structure.
- Attempted to run `pwsh -File ./dev.ps1 bootstrap`, but PowerShell was not available in the container (`pwsh: command not found`).
