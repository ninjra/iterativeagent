# Agent Instructions

- Keep changes small and focused.
- Always run `./dev.ps1 check` before declaring work done.
- Update `WORKLOG.md` with what changed and why.
- Add or adjust tests when behavior changes.
- Avoid introducing network calls or telemetry by default.
